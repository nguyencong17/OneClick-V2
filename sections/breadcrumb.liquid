{% render 'breadcrumb' %}

{% schema %}
{
  "name": "t:sections.breadcrumb.name",
  "class": "section",
  "tag": "section",
  "presets": [
    {
      "name": "t:sections.breadcrumb.name",
      "category": "Text"
    }
  ]
}
{% endschema %}
